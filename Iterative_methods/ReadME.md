Iterative method is used to solve Ax =b in computers, where A is very large, to find x(approximate solution) by iterations.

Usually only 2 types of operations are used in iteration, matrix multiplication with vectors, and sum.

it is typically more favorable than other methods which includes steps to do inversion of matrix, division, etc.
